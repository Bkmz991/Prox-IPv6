# 📤 Инструкция по загрузке на GitHub

## 🎯 Цель
Загрузить проект на GitHub, чтобы его можно было установить одной командой:

```bash
sudo yum install git python3 openssl -y && \
rm -rf Prox && \
git clone https://github.com/Bkmz991/Prox.git && \
cd Prox && \
chmod +x setup.py && \
python3 setup.py
```

---

## 📋 Шаг 1: Подготовка (уже выполнено ✅)

Все файлы готовы в папке: `C:\Users\user\Desktop\zp\prox\`

```
prox/
├── setup.py              # ✨ Новый главный скрипт
├── NPPRPROXY.sh          # Улучшенный установщик
├── NPPRPROXY.sh.enc      # Зашифрованная версия
├── README.md             # Документация
├── IMPROVEMENTS.md       # Описание улучшений
├── CHEATSHEET.md         # Шпаргалка команд
├── CHANGELOG.md          # История изменений
├── SUMMARY.md            # Краткая сводка
├── .gitignore            # ✨ Новый - игнорируемые файлы
└── GITHUB_UPLOAD.md      # Эта инструкция
```

---

## 📤 Шаг 2: Создание репозитория на GitHub

### Вариант A: Через веб-интерфейс

1. Перейдите на https://github.com/Bkmz991
2. Нажмите **"New"** (создать новый репозиторий)
3. Укажите имя: **Prox**
4. Описание: **IPv6 Proxy Generator with rotation and anonymity features**
5. Выберите: **Public** (публичный)
6. ✅ НЕ добавляйте README, .gitignore, license (у нас уже есть)
7. Нажмите **"Create repository"**

---

## 💻 Шаг 3: Загрузка файлов

### Откройте PowerShell в папке проекта:

```powershell
cd C:\Users\user\Desktop\zp\prox
```

### Инициализируйте Git репозиторий:

```powershell
git init
git add .
git commit -m "v2.0 - Enhanced anonymity and security features"
```

### Подключите к GitHub:

```powershell
git branch -M main
git remote add origin https://github.com/Bkmz991/Prox.git
```

### Загрузите файлы:

```powershell
git push -u origin main
```

**Примечание:** GitHub попросит авторизацию:
- Используйте Personal Access Token (не пароль!)
- Создать токен: https://github.com/settings/tokens
- Права: `repo` (полный доступ к репозиториям)

---

## ✅ Шаг 4: Проверка

После загрузки проверьте:

1. Откройте: https://github.com/Bkmz991/Prox
2. Убедитесь, что все файлы загружены
3. Проверьте, что README.md отображается на главной странице

---

## 🚀 Шаг 5: Тестирование установки

На чистом Linux сервере выполните:

```bash
sudo yum install git python3 openssl -y && \
rm -rf Prox && \
git clone https://github.com/Bkmz991/Prox.git && \
cd Prox && \
chmod +x setup.py && \
python3 setup.py
```

**Ожидаемый результат:**
- Скачивание репозитория ✅
- Запуск setup.py ✅
- Проверка прав root ✅
- Запуск NPPRPROXY.sh ✅
- Интерактивная установка прокси ✅

---

## 🔄 Обновление репозитория (в будущем)

Если внесёте изменения:

```powershell
cd C:\Users\user\Desktop\zp\prox
git add .
git commit -m "Описание изменений"
git push
```

---

## 📝 Рекомендации для README.md на GitHub

GitHub автоматически покажет ваш README.md на главной странице. Он уже готов и содержит:

- ✅ Описание проекта
- ✅ Быстрый старт
- ✅ Системные требования
- ✅ Примеры использования
- ✅ FAQ
- ✅ Контакты NPPRTEAM

---

## 🏷️ Опционально: Добавить темы (topics)

На странице репозитория добавьте темы для лучшей находимости:

```
ipv6
proxy
proxy-server
anonymity
rotation
privacy
3proxy
centos
linux
socks5
http-proxy
```

**Как добавить:**
1. На странице репозитория нажмите ⚙️ возле "About"
2. В поле "Topics" добавьте темы через запятую
3. Сохраните

---

## 📜 Опционально: Добавить лицензию

Рекомендуется MIT License:

1. На GitHub: Add file → Create new file
2. Имя файла: `LICENSE`
3. Нажмите "Choose a license template"
4. Выберите "MIT License"
5. Заполните год и имя (NPPRTEAM)
6. Commit

---

## 🔒 Важно: NPPRPROXY.sh.enc

Зашифрованный файл `NPPRPROXY.sh.enc` тоже будет загружен. Это нормально, если вы хотите сохранить зашифрованную версию для резерва.

Если НЕ хотите его публиковать:

1. Добавьте в `.gitignore`:
   ```
   NPPRPROXY.sh.enc
   ```

2. Удалите из Git:
   ```powershell
   git rm --cached NPPRPROXY.sh.enc
   git commit -m "Remove encrypted file"
   git push
   ```

---

## 🎉 Готово!

После загрузки ваш репозиторий будет доступен по адресу:

**https://github.com/Bkmz991/Prox**

И устанавливаться одной командой:

```bash
sudo yum install git python3 -y && \
git clone https://github.com/Bkmz991/Prox.git && \
cd Prox && \
python3 setup.py
```

---

## 🆘 Проблемы и решения

### Проблема: "Permission denied"
```powershell
git config --global user.email "your-email@example.com"
git config --global user.name "Bkmz991"
```

### Проблема: "Authentication failed"
- Используйте Personal Access Token, не пароль
- Создать: https://github.com/settings/tokens
- При push введите токен вместо пароля

### Проблема: "Repository already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/Bkmz991/Prox.git
git push -u origin main --force
```

---

## 📞 Нужна помощь?

Если возникли проблемы:
- GitHub Docs: https://docs.github.com
- Git Guide: https://git-scm.com/doc

---

**Удачи с загрузкой! 🚀**


