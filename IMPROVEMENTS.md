# 🔐 Улучшения NPPRPROXY для повышенной анонимности

## ✅ Применённые изменения

### 1️⃣ **Локальное хранение файлов вместо file.io**

**Проблема:** Файлы с прокси загружались на сторонний сервис file.io, что создавало риски безопасности.

**Решение:** 
- Файлы теперь сохраняются локально на сервере в `/root/proxy_exports/`
- Создаётся ZIP-архив с паролем
- Файлы защищены правами доступа (chmod 600)
- Уникальные имена файлов с временными метками

**Как скачать:**
```bash
scp root@YOUR_SERVER_IP:/root/proxy_exports/proxy_*.zip ./
```

---

### 2️⃣ **Защита от DNS утечек**

**Проблема:** DNS запросы могли уходить напрямую к провайдеру, раскрывая реальный IP.

**Решение:**
- Блокировка всех DNS запросов через iptables (кроме root)
- Принудительное использование локального dnsmasq
- Защита как для IPv4, так и для IPv6

**Код:**
```bash
iptables -A OUTPUT -p udp --dport 53 -j DROP
ip6tables -A OUTPUT -p udp --dport 53 -j DROP
```

---

### 3️⃣ **Защита от WebRTC утечек**

**Проблема:** WebRTC может раскрывать реальный IP через STUN/TURN серверы.

**Решение:**
- Блокировка портов 3478 и 19302 (стандартные STUN порты)
- Предотвращает утечку локальных и внешних IP

**Код:**
```bash
iptables -A OUTPUT -p udp --dport 3478 -j DROP
iptables -A OUTPUT -p udp --dport 19302 -j DROP
```

---

### 4️⃣ **Оптимизация MTU для IPv6**

**Проблема:** Неправильный MTU может создавать уникальный fingerprint сети.

**Решение:**
- Установка стандартного MTU 1280 для IPv6
- Оптимизация диапазона портов (10000-65000)
- Включение переиспользования TIME_WAIT сокетов

**Код:**
```bash
net.ipv6.conf.all.mtu = 1280
net.ipv4.ip_local_port_range = 10000 65000
net.ipv4.tcp_tw_reuse = 1
```

---

### 5️⃣ **Отключение логирования в 3proxy**

**Проблема:** Логи прокси-сервера могут содержать конфиденциальную информацию.

**Решение:**
- Добавлена директива `nolog` в конфигурацию 3proxy
- Никакая активность прокси не записывается

**Код:**
```bash
daemon
nserver 127.0.0.1
nolog  # <--- Новая строка
users ...
```

---

### 6️⃣ **Улучшенный TCP/IP Fingerprinting**

**Проблема:** Базовые настройки TCP не полностью имитировали разные ОС.

**Решение:**
- Добавлены параметры window_scaling, timestamps, sack
- Настройка буферов rmem/wmem для Windows
- Более точная имитация всех 5 типов устройств

**Поддерживаемые ОС:**
1. Windows 10/11 (TTL=128)
2. MacOS (TTL=64, быстрый fin_timeout)
3. Linux (TTL=64, стандартные параметры)
4. Android (TTL=64, короткий keepalive)
5. iPhone/iOS (TTL=64, SACK включен)

---

## 📊 Сравнение до/после

| Параметр | До | После |
|----------|----|----|
| **Хранение файлов** | ❌ file.io (публично) | ✅ Локально на сервере |
| **DNS утечки** | ⚠️ Возможны | ✅ Заблокированы |
| **WebRTC утечки** | ⚠️ Возможны | ✅ Заблокированы |
| **Логирование** | ⚠️ Включено | ✅ Отключено |
| **TCP fingerprint** | ⚠️ Базовый | ✅ Продвинутый |
| **MTU оптимизация** | ❌ Нет | ✅ Да |

---

## 🚀 Как использовать

### 1. Запуск скрипта:
```bash
chmod +x NPPRPROXY.sh
sudo ./NPPRPROXY.sh
```

### 2. Ответьте на вопросы:
- Количество прокси (например: 500)
- TCP/IP отпечаток (1-5)
- Тип прокси (статические/ротация)
- Логины (один для всех/разные)

### 3. Скачайте файлы:
```bash
# Посмотреть список файлов
ssh root@YOUR_SERVER ls -la /root/proxy_exports/

# Скачать архив
scp root@YOUR_SERVER:/root/proxy_exports/proxy_20251027_143052.zip ./

# Распаковать (пароль будет показан в конце установки)
unzip -P PASSWORD proxy_20251027_143052.zip
```

---

## 🔒 Рекомендации по безопасности

1. **Удаляйте старые файлы** из `/root/proxy_exports/` после скачивания
2. **Используйте SSH ключи** вместо паролей для доступа к серверу
3. **Меняйте пароли** к ZIP-архивам регулярно
4. **Ограничьте SSH доступ** только с определённых IP
5. **Используйте разные логины** для каждого прокси (опция 2)

---

## 📝 Дополнительные команды

### Просмотр активных прокси:
```bash
netstat -tulpn | grep 3proxy
```

### Перезапуск прокси-сервера:
```bash
systemctl restart 3proxy
```

### Просмотр конфигурации:
```bash
cat /usr/local/etc/3proxy/3proxy.cfg
```

### Удаление всех экспортированных файлов:
```bash
rm -rf /root/proxy_exports/*
```

---

## 🎯 Итоговый уровень анонимности

✅ **Высокий уровень защиты:**
- IPv6 ротация на каждый запрос
- Отсутствие DNS утечек
- Блокировка WebRTC
- Имитация различных устройств
- Отсутствие логов
- Локальное хранение данных

⚠️ **Что ещё можно улучшить:**
- Добавить автоматическую очистку старых файлов
- Внедрить ротацию User-Agent
- Добавить поддержку DoH (DNS over HTTPS)
- Интегрировать fail2ban для защиты от брутфорса

---

**Создано:** 27 октября 2025  
**Версия:** 2.0 (улучшенная анонимность)  
**Автор модификаций:** AI Assistant + User


