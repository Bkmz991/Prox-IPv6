# 🚀 IPv6 Proxy Generator с ротацией IP

Автоматизированный скрипт для создания приватных IPv6 прокси-серверов с функцией ротации IP-адресов и имитацией различных устройств.

## ⚡ Быстрая установка (одна команда)

```bash
sudo yum install git python3 -y && \
git clone https://github.com/Bkmz991/Prox.git && \
cd Prox && \
python3 setup.py
```

**Требования:** CentOS/RHEL/Rocky Linux с IPv6 подсетью /64 или больше

---

## 📁 Структура проекта

```
prox/
├── setup.py              # 🔥 Главный скрипт установки (рекомендуется)
├── NPPRPROXY.sh          # Основной bash-скрипт установки
├── NPPRPROXY.sh.enc      # Зашифрованная версия (AES-256-CBC)
├── README.md             # Этот файл
├── IMPROVEMENTS.md       # Подробное описание улучшений
├── CHEATSHEET.md         # Шпаргалка команд
├── CHANGELOG.md          # История изменений
├── SUMMARY.md            # Краткая сводка
└── GITHUB_UPLOAD.md      # Инструкция по загрузке на GitHub
```

---

## ⚡ Быстрый старт

### ⭐ Рекомендуемый способ: setup.py
```bash
# После клонирования репозитория
cd Prox
sudo python3 setup.py
```

**Что происходит:**
1. Проверка прав root ✅
2. Проверка системных требований ✅
3. Установка прав на выполнение ✅
4. Запуск NPPRPROXY.sh ✅

---

### Альтернативный способ: Прямой запуск
```bash
chmod +x NPPRPROXY.sh
sudo ./NPPRPROXY.sh
```

---

## 🎮 Интерактивная настройка

Скрипт задаст вам следующие вопросы:

### 1. Количество прокси
```
Сколько прокси вы хотите создать? Пример 500
> 500
```

### 2. TCP/IP Отпечаток (Fingerprint)
```
Выберите TCP/IP Отпечаток для ваших прокси:
1 - Windows
2 - MacOS
3 - Linux
4 - Android
5 - iPhone
> 1
```

### 3. Тип прокси
```
Какие прокси вы хотите создать?
1 - Статические (фиксированный IP для каждого порта)
2 - С ротацией (новый IP на каждый запрос)
> 2
```

### 4. Авторизация
```
Вы хотите создать один логин и пароль для всех прокси, или разные?
1 - Один (удобно)
2 - Разные (безопаснее)
> 2
```

---

## 📦 Формат выходных файлов

### Файл proxy.txt:
```
IP:PORT:USERNAME:PASSWORD
123.45.67.89:10000:abc12:xyz34
123.45.67.89:10001:def56:uvw78
123.45.67.89:10002:ghi90:rst12
```

### Типы прокси:
- **HTTP прокси:** порты 10000+
- **SOCKS5 прокси:** порты 30000+ (порт HTTP + 20000)

---

## 🔐 Улучшения безопасности (v2.0)

### ✅ Что было исправлено:

1. **Локальное хранение** - файлы больше не загружаются на file.io
2. **Защита от DNS leak** - блокировка прямых DNS запросов
3. **Защита от WebRTC leak** - блокировка STUN/TURN
4. **Отключение логов** - прокси не записывает активность
5. **Улучшенный TCP fingerprint** - точная имитация устройств
6. **Оптимизация MTU** - правильные настройки для IPv6

📖 Подробности: см. [IMPROVEMENTS.md](IMPROVEMENTS.md)

---

## 📥 Как скачать файлы с сервера

### 1. Посмотреть список файлов:
```bash
ssh root@YOUR_SERVER_IP "ls -lah /root/proxy_exports/"
```

### 2. Скачать архив:
```bash
scp root@YOUR_SERVER_IP:/root/proxy_exports/proxy_20251027_143052.zip ./
```

### 3. Распаковать (пароль показан в конце установки):
```bash
unzip -P YOUR_PASSWORD proxy_20251027_143052.zip
```

---

## ⚙️ Системные требования

### Операционная система:
- **CentOS 7/8/9** (Полная поддержка CentOS 9 Stream)
- **RHEL 7/8/9**
- **Rocky Linux 8/9**
- **AlmaLinux 8/9**

### Сеть:
- **IPv6 подсеть /64 или больше** (/48, /32)
- **Важно для Linode:** В панели управления Linode необходимо добавить **IPv6 Range (/64)** во вкладке Networking. Без этого прокси работать не будут.
- Активное IPv6 подключение

### Ресурсы (для 500 прокси):
- CPU: 2+ ядра
- RAM: 2+ GB
- Диск: 5+ GB

---

## 🛠️ Технические детали

### Используемое ПО:
- **3proxy v0.9.3** - прокси-сервер
- **dnsmasq** - локальный DNS
- **iptables/ip6tables** - фильтрация трафика
- **OpenSSL** - шифрование (для npprproxy.py)

### Автоматическая настройка:
- Определение IPv6 подсети
- Генерация случайных IPv6 адресов
- Настройка TCP/IP параметров
- Конфигурация firewall
- Создание systemd сервиса

---

## 📊 Примеры использования

### Curl через HTTP прокси:
```bash
curl -x http://abc12:xyz34@123.45.67.89:10000 https://api.ipify.org?format=json
```

### Curl через SOCKS5 прокси:
```bash
curl -x socks5://abc12:xyz34@123.45.67.89:30000 https://api.ipify.org?format=json
```

### Python requests:
```python
import requests

proxies = {
    'http': 'http://abc12:xyz34@123.45.67.89:10000',
    'https': 'http://abc12:xyz34@123.45.67.89:10000'
}

response = requests.get('https://api.ipify.org?format=json', proxies=proxies)
print(response.json())
```

---

## 🔧 Управление сервисом

### Статус прокси:
```bash
systemctl status 3proxy
```

### Перезапуск:
```bash
systemctl restart 3proxy
```

### Остановка:
```bash
systemctl stop 3proxy
```

### Просмотр активных соединений:
```bash
netstat -tulpn | grep 3proxy
```

---

## 🧹 Обслуживание

### Удалить старые экспортированные файлы:
```bash
rm -f /root/proxy_exports/proxy_*.zip
rm -f /root/proxy_exports/proxy_*.txt
```

### Очистить временные файлы:
```bash
rm -rf /home/proxy-installer
```

### Полное удаление:
```bash
systemctl stop 3proxy
systemctl disable 3proxy
rm -rf /3proxy /usr/local/etc/3proxy /root/proxy_exports /home/proxy-installer
```

---

## ❓ FAQ

### Q: Сколько прокси можно создать?
**A:** Зависит от размера IPv6 подсети:
- /64: ~18 квинтиллионов адресов
- /48: практически безграничное количество

### Q: Работает ли на Debian/Ubuntu?
**A:** Скрипт заточен под CentOS/RHEL (yum). Для Debian нужно заменить `yum` на `apt`.

### Q: Можно ли использовать без IPv6?
**A:** Нет, скрипт требует наличия IPv6 подсети.

### Q: Как проверить свой IPv6?
```bash
curl -6 https://api64.ipify.org
ip -6 addr show
```

### Q: Прокси медленные, что делать?
- Проверьте MTU: `ip -6 link show`
- Проверьте загрузку: `htop`
- Уменьшите количество прокси

---

## 🔗 Контакты NPPRTEAM

- **Telegram:** https://t.me/nppr_team
- **VK:** https://vk.com/npprteam
- **Магазин (Telegram):** https://t.me/npprteamshop
- **Магазин (Web):** https://npprteam.shop
- **Antik Browser:** https://antik-browser.com/

---

## ⚠️ Ответственность

Используйте прокси только в законных целях:
- ✅ Тестирование веб-приложений
- ✅ Парсинг данных (в рамках TOS)
- ✅ Обход геоблокировок (где разрешено)
- ❌ DDoS атаки
- ❌ Мошенничество
- ❌ Спам

---

**Версия:** 2.0  
**Дата обновления:** 27 октября 2025  
**Лицензия:** MIT (с сохранением авторства NPPRTEAM)

